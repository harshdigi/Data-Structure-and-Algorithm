#include<bits/stdc++.h>
using namespace std;
vector<int> sol (vector<int>a, int n){
    vector<int> right(n), left(n);
    
    return right;
}
int main()
{
    int n;
    vector<int> a;
    cin>>n;
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        a.push_back(x);
    }
    sol(a, n);
    return 0;
}